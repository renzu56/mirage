# Public (safe in the browser)
NEXT_PUBLIC_SUPABASE_URL=
# Use ONE of these (anon/publishable key)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_KEY=

# Server-only (NEVER expose to the browser)
# Use ONE of these (new secret key or legacy service role)
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# fallback if you insist on this name
SUPABASE_KEY=

# Used to hash IPs for like tracking
LIKE_SALT=change-me-to-a-random-string
